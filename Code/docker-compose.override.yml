version: "3.4"

services:
  amped.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
    ports:
      - "5001:443"
    volumes:
      - ~/.aspnet/https:/https:ro